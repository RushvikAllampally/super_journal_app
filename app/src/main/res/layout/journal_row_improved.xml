<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="?attr/colorSurface">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Journal Type Icon -->
        <ImageView
            android:id="@+id/journal_type_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:tint="@color/text_primary"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_diary" />

        <!-- Journal Title -->
        <TextView
            android:id="@+id/journal_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:fontFamily="@font/balsamiqsans_regular"
            android:maxLines="1"
            android:textColor="?attr/colorOnSurface"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@+id/journal_lock"
            app:layout_constraintStart_toEndOf="@+id/journal_type_icon"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="My First Journal Entry" />

        <!-- Lock Icon -->
        <ImageView
            android:id="@+id/journal_lock"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_lock"
            android:visibility="gone"
            app:tint="@color/text_primary"
            app:layout_constraintEnd_toStartOf="@+id/journal_bookmark"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Bookmark Icon -->
        <ImageView
            android:id="@+id/journal_bookmark"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_bookmark"
            app:tint="@color/text_primary"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Journal Content Preview -->
        <TextView
            android:id="@+id/journal_content"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:fontFamily="@font/balsamiqsans_regular"
            android:maxLines="2"
            android:textColor="?attr/colorOnSurface"
            android:alpha="0.8"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/journal_type_icon"
            app:layout_constraintTop_toBottomOf="@+id/journal_title"
            tools:text="Today I went to the park and saw some beautiful flowers..." />

        <!-- Bottom Row: Date and Tags -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/journal_content">

            <!-- Date with calendar icon -->
            <TextView
                android:id="@+id/journal_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawablePadding="4dp"
                android:fontFamily="@font/balsamiqsans_regular"
                android:gravity="center_vertical"
                android:textColor="?attr/colorOnSurface"
                android:alpha="0.7"
                android:textSize="12sp"
                app:drawableStartCompat="@android:drawable/ic_menu_my_calendar"
                app:drawableTint="@color/text_primary"
                tools:text="Oct 5, 2025" />

            <!-- Spacer -->
            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <!-- Tags container (horizontal scroll for multiple tags) -->
            <HorizontalScrollView
                android:id="@+id/tags_scroll"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scrollbars="none">

                <com.google.android.material.chip.ChipGroup
                    android:id="@+id/journal_tags_group"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:chipSpacing="4dp"
                    app:singleLine="true">

                    <!-- Tags will be added here programmatically -->
                    <com.google.android.material.chip.Chip
                        android:id="@+id/journal_tag"
                        android:layout_width="wrap_content"
                        android:layout_height="24dp"
                        android:textSize="10sp"
                        android:textColor="@color/text_primary"
                        app:chipBackgroundColor="?attr/colorSurface"
                        app:chipIcon="@drawable/tag_24"
                        app:chipIconSize="12dp"
                        app:chipIconTint="@color/text_primary"
                        app:chipMinHeight="24dp"
                        app:chipStrokeColor="@color/text_primary"
                        app:chipStrokeWidth="1dp"
                        app:ensureMinTouchTargetSize="false"
                        tools:text="Personal" />
                </com.google.android.material.chip.ChipGroup>
            </HorizontalScrollView>
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
